// some verifica se pelo menos um elemento satisfaz a condição da função
let numeros = [1, 2, 3];
let resultado = numeros.some(num => num > 2); // Verifica se algum número é maior que 2

console.log(resultado); // Exibe: true