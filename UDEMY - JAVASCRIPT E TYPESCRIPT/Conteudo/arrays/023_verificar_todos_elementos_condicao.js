// every verifica se todos os elementos satisfazem a condição da função
let numeros = [1, 2, 3];
let resultado = numeros.every(num => num > 0); // Verifica se todos são maiores que 0

console.log(resultado); // Exibe: true